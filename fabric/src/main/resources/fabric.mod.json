{
  "schemaVersion": 1,
  "id": "${MODID}",
  "name": "${NAME}",
  "version": "${VERSION}",
  "environment": "client",
  "description": "${DESCRIPTION}",
  "authors": ["${AUTHOR}"],
  "contact": {
    "homepage": "",
    "sources": "",
    "issues": ""
  },
  "license": "MIT",
  "icon": "assets/rougher_environment/icon.png",
  "entrypoints": {
    "client": [
      "com.real_sebastiago.rougher_environment.Rougher_Environment"
    ]
  },
  "depends": {
    "fabricloader": ">=${FABRIC_LOADER}",
    "java": ">=${JAVA}",
    "minecraft": "${MINECRAFT}"
  },
  "recommends": {
    "fabric": "*"
  },
  "mixins": [
    {
      "config": "rougher_environment.mixins.json",
      "environment": "client"
    },
    {
      "config": "rougher_environment.fabric.mixins.json",
      "environment": "client"
    }
  ]
}